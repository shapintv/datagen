framework:
    secret: 'ShapinRocks!'
    test: ~
    session:
        storage_id: 'session.storage.mock_file'

doctrine:
    dbal:
        driver: 'pdo_sqlite'
        path: '%kernel.cache_dir%/db.sqlite'
        charset: 'UTF8'

shapin_datagen:
    groups:
        default: '%kernel.project_dir%/../Fixtures/TestBundle/Datagen/DBAL'

services:
    logger:
        class: Psr\Log\NullLogger
